name: Keep Alive
on:
  schedule:
    - cron: "0 8 * * *"   # daily at 8 AM UTC
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s -o /dev/null -w "%{http_code}" ${{ secrets.APP_URL }} || true
